---
layout: archive
pageable: true
per_page: 10
---

<% case page_type
   when 'day'
     date = prettify_date(Date.new(year, month, day))
   when 'month'
     date = Date.new(year, month, 1).strftime('%b, %Y')
   when 'year'
     date = year
     date = date.to_s
    end %>
<% @title = 'Archive for ‘' + date + '’' %>
<% @description = 'Summaries of all articles from <em>' + date + '</em>' %>